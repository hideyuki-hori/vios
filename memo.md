# sticky メモ 仕様

`m` でページ右下にグローバルな付箋パネルを開閉する。

## 位置づけ

2026-08-02 の「メモ機能は vios に入れない」決定は、vim-lite エディタ + IndexedDB + 一覧・検索 を前提にしたもの。
本機能は以下に縮小することでその懸念(永続データの重さ・全ページ注入コスト)を回避する。

- エディタは素の `<textarea>` のみ
- 保存は `chrome.storage.local`
- 一覧・検索・markdown・同期・エクスポートは持たない
- パネルの DOM は初回に `m` を押すまで生成しない

## スコープ

- グローバル。どのページで開いても同じ付箋群が見える
- 付箋は複数枚。1 枚ずつ独立した浮遊ボックスで、ドラッグで位置を動かせる(2026-08-24 に 1 パネル縦積みから変更)
- ページ単位・ドメイン単位の紐づけはしない

## 表示

- 各付箋は `position: fixed`、既定 260×140px。上部のグリップをドラッグで移動(ビューポート内にクランプ)、右下角のハンドルでリサイズ(最小 140×80px)。本文はサイズ内でスクロール
- 位置・サイズは付箋ごとに `x` / `y`(ビューポート左上基準 px)/ `w` / `h` で保存。新規付箋は右上を起点に 24px ずつずらして置く
- フォーカス中の付箋を最前面(z-index)にする
- `palette.css` とトーンを揃える
- 開閉状態は保存しない。ページ読み込み時は常に閉じている(`m` で全付箋をまとめて表示 / 非表示)

## キー操作

| キー | 動作 |
| --- | --- |
| `m` (通常モード) | パネル開閉トグル。開いたら最後に触った付箋へフォーカス。付箋が 0 枚なら 1 枚作ってフォーカス |
| `Esc` | パネルを閉じる |
| `Ctrl+n` | 新規付箋を末尾に追加してフォーカス |
| `Ctrl+j` / `Ctrl+k` | 次 / 前の付箋へフォーカス移動 |
| `Ctrl+d` | フォーカス中の付箋を削除。空なら即削除、本文ありなら 2 回連続押しで削除 |

- textarea 内では vios の通常キーバインドは無効(`shouldIgnore` に乗る)。`m` は文字として入力される
- 他のモーダル(オムニバー / タブスイッチャー / ブックマーク / ヒント)表示中は `m` を無視する
- 修飾キーは `Ctrl` 起点で仮置き。サイトと衝突したら `Alt` に寄せる

## 永続化

```ts
type Memo = { id: string; text: string; updatedAt: number; x: number; y: number; w: number; h: number }
type MemoState = { memos: Memo[] }
```

- `chrome.storage.local` のキー `memoState`
- `id` は `crypto.randomUUID()`
- 順序は配列順(`Ctrl+j` / `Ctrl+k` の移動順)。並べ替えなし
- `x` / `y` / `w` / `h` を欠く旧データは (16, 16) / 260×140 を補って読む
- 入力はデバウンス(300ms 程度)で保存、閉じる時にも確定
- 空文字の付箋は閉じる時に削除する(`m` で自動生成した 1 枚も同様)
- `unlimitedStorage` は付けない

## 複数タブ

- `chrome.storage.onChanged` を購読し、他タブの変更をパネルに反映する
- フォーカス中の付箋は上書きしない(編集中の内容を守る)。それ以外の付箋は差し替える
- 他タブで削除された付箋にフォーカスしていた場合はそのまま残し、次の保存で再登録される

## 持たないもの(後から足せる)

- z 順の永続化
- 一覧・検索・`M` での付箋一覧パレット
- markdown 描画、色分け、並べ替え
- `storage.sync`、エクスポート

## ファイル構成(予定)

- `src/content/memo.core.ts` 状態遷移(追加 / 削除 / フォーカス移動 / 他タブ差分の取り込み)。純粋
- `src/content/memo.core.test.ts`
- `src/content/memo.storage.ts` `chrome.storage.local` の読み書きと `onChanged` 購読
- `src/content/memo.view.ts` パネル DOM とキーハンドリング
- `src/content/memo.css`
- `keybinds.core.ts` に `openMemo` を追加、`actions.ts` / `index.ts` に配線
